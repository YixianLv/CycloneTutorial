#include "TempControlSplDcps.h"
#include "TempControl_DCPS.hpp"

#include <org/eclipse/cyclonedds/core/EntityRegistry.hpp>

void
__tutorial_TempSensorType__copyIn(
    const void *_from,
    void *_to)
{
    const class ::tutorial::TempSensorType *from;
    tutorial_TempSensorType *to;

    from = (const class ::tutorial::TempSensorType *)_from;
    to = (tutorial_TempSensorType *)_to;

    to->id = (int16_t)(from->id());
    to->temp = (float)(from->temp());
    to->hum = (float)(from->hum());
    to->scale = (tutorial_TemperatureScale)from->scale();

}

void
__tutorial_TempSensorType__copyOut(
    const void *_from,
    void *_to)
{
    ::tutorial::TempSensorType *to = (::tutorial::TempSensorType *)_to;
    const tutorial_TempSensorType *from = (const tutorial_TempSensorType *)_from;

    (*to).id() = (int16_t)from->id;
    (*to).temp() = (float)from->temp;
    (*to).hum() = (float)from->hum;
        /* Code generated by /home/yixian/Desktop/mygit/idlpp-cxx/idlpp/ppresources/ospli/src/tools/idlpp/code/idl_genLiteISOCxxCopyout.c at line 383 */
    (*to).scale() = (::tutorial::TemperatureScale)from->scale;
}

